# ASHFALL development guidance — beta 0.2.9

This performance release preserves beta 0.2.8 visual output and gameplay. Keep actual CPU aperture restriction, full-width sprite depth sampling, zero Transfer Canvas readbacks, water-refraction dependency rows, reflected-camera culling, prepared crossing caches, and exact topology invalidation. Do not replace these with frozen doorway screenshots, geometry cuts or lower quality as a substitute for removing redundant work. `performance-preparation.js` must finish through the existing loading gate before `artReady`; `performance-metrics.js` is opt-in and local-only. Run focused work/parity/lifecycle gates when changing these paths. A browser FPS claim requires a browser/device measurement; native Canvas work counts and image comparisons are separate evidence.

The owner's latest direction makes the Drowned Hollow the only rare threat in the Lower Restrooms. It retains the approved hollow face, roughly four-metre height, reservoir placement and durability. Its body is swollen, waterlogged and decayed, with a closed abdomen, rounded empty hands and no blade. Its slower charge, readable attack and recovery must give the player room to dodge and retaliate. No discontinued creature, image, audio, preview or spawn logic belongs in this release. Old checkpoints discard retired encounters without healing, rerolls, rewards or lost progress. Minimal validation/migration of historical data is allowed only to remove it safely.

Preserve all 84 rooms across six districts and six lower floors, the separate chapter below the Heart, mobile input, full vertical aiming and projectile water reflections. The Restrooms are reached through the visible stair at the end of the existing post-Heart left corridor; right remains normal discharge. Transfer completion chooses left Warden guidance. The Warden stays off-path and disappears in place. Both ends of the flooded Transfer show the actual adjoining spaces with continuous camera, input, weapon and momentum. Its approved interior, optical compression, forward-only movement, invulnerability and wall screams remain intact.

Passing the bottom stair doorway commits a one-way descent; ordinary backward movement inside the restrooms remains. The hidden deep sewer exits between the hospital and lake into the existing Cerberus campaign. The exterior culvert stays barred on ordinary arrival until earned from below. Preserve health, ammo, exploration, doors, rewards, checkpoint resources and preview isolation.

`src/modules.json` defines ordered runtime units sharing lexical state. Edit source, then regenerate root `index.html` and `assets/code` with `tools/build.py`. Preserve all 29 inherited media payloads and original cue constants exactly. The approved `restroom-audio.js` and `restroom-water-samples.js` remain byte-identical to beta 0.2.6/0.2.7. Supplemental retired artwork must not remain in the source package. No deployment is included.

Run distribution/media integrity, asset-loader, mobile/HUD, Transfer, restroom traversal, audio, combat, rendering and save checks relevant to the release. Distinguish native simulation and captured frames from real browser/phone playthroughs and human listening. Keep temporary QA, redundant standalone exports, machine paths and logs out of the web/source ZIP. `LOWER_RESTROOMS_PLAN.md` preserves the historical architectural design; current owner instructions above supersede its earlier encounter and ending proposals.
